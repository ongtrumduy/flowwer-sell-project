{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Express with ts-node",
      "program": "${workspaceFolder}/server.ts", // Đường dẫn đến file chính
      "runtimeArgs": [
        "-r",
        "ts-node/register" // Đăng ký ts-node
      ],
      "console": "integratedTerminal"
    }
  ]
}
